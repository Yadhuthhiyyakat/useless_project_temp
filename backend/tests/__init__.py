"""Tests package for the Digital Cemetery backend.

The presence of this ``__init__.py`` lets pytest prepend the project root to
``sys.path`` so ``import app`` works regardless of the installed environment.
"""